/// MCP DevTools server — re-exported from services for convenience.
///
/// The actual implementation lives in `lib/services/devtools_server.dart`
/// and `lib/services/mcp/`. See [DevtoolsServer] for the main entry point.
library;

export '../services/devtools_server.dart';
export '../services/mcp/tool_registry.dart' show TerminalProvider;
